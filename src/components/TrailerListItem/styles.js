import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	trailerContainer: {
		display: 'flex',
		justifyContent: 'center',
		height: 370,
		width: 380,
		marginVertical: 10,
		borderRadius: 20,
		overflow: 'hidden'
	}
});